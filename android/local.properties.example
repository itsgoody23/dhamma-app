# Copy this file to local.properties and fill in the values.
# local.properties is NOT committed to version control.

# Path to your Flutter SDK installation:
flutter.sdk=/path/to/flutter

# These are set automatically by flutter build/run — you normally don't need to set them:
# flutter.versionCode=1
# flutter.versionName=1.0.0
